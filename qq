#include <iostream>
#include <string>
using namespace std;

int main()
{
	string a; int z;
	getline(cin, a);
	for (int i = 0; i < a.length(); i++) {
		if (a[i] == ' ' && a[i - 1] == 'x') a[i - 1] = 'z';
			
	}
	cout << a;
}
